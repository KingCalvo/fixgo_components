name: fixgo_components
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  image_picker: ^1.0.7
  permission_handler: ^11.3.1
  table_calendar: ^3.0.9
  intl: ^0.19.0
  flutter_form_builder: ^9.2.1
  flutter_map: ^6.1.0
  latlong2: ^0.9.0

flutter:
  uses-material-design: true
